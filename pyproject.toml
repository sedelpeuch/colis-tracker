[project]
name = "colis-tracker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "jinja2>=3.1",
    "python-multipart>=0.0.9",
    "python-barcode[images]>=0.15",
]

[dependency-groups]
dev = [
    "ty>=0.0.1a0",
]

[tool.ty.rules]
